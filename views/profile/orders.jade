extends info

block details

    table.content-table
        thead
            tr
                th(colspan='5') Historial de Ordenes
        thead
            tr
                td #
                td Fecha
                td Estatus actual
                td Total
                td    

        tbody
            if (orders < 1)
                tr
                    td
                    td(colspan="3", style="text-align: center;")
                        p= "No se encontraron ordenes registradas"
                        a(class={active: currentUrl === '/'}, href='/')
                                            | Explorar productos
            else
                each order in orders
                    tr
                        td= order.OrderID
                        td= order.OrderDate
                        td= order.Status
                        td(style='color: green;')= ' $' + order.Total
                        td
                            form(action='/usr/' + customer.Username + '/orders/' + order.OrderID, method='GET')
                                button.btn.btn-default.order-btn(type='submit' class="btn btn-link")
                                    i.fa.fa-eye
                                    = ' Detalle de orden'